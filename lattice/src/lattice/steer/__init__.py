"""STEER — Organ 4.

Atom embeddings summed into a steering vector applied to the LLM's
residual stream at selected layers (open-weight path) or rendered as a
compact priming preamble (hosted fallback). Steering vectors get
Hebbian-EMA updates from outcome.
"""
